#include "../../include/cub3d.h"

int	extract_texture(t_engine *engine, char *line, char *dir)
{
	//char	*extracted;

	(void)engine;
	(void)dir;
	printf("I'm here!!!\n");
	printf("%s", line);
	return (0);
}
